# Database connection string
# For local development with SQLite: DATABASE_URL="file:./dev.db"
# For PostgreSQL (Neon, Supabase, etc.): DATABASE_URL="postgresql://user:password@host/database?sslmode=require"
DATABASE_URL="postgresql://user:password@host/database?sslmode=require"
